services:
  openviking:
    build: .
    ports:
      - "1933:1933"
    environment:
      - OPENVIKING_CONFIG_FILE=/data/ov.conf
    volumes:
      - ./ov.conf:/data/ov.conf:ro
      - ./data:/data/openviking
